source 'https://rubygems.org'

# Don't pull from the gemspec — the gemspec lists posix_mq which won't build everywhere
gem 'test-unit', '~> 3.0'
gem 'io-metrics', '>= 0.3.0'
gem 'raindrops', path: '.'
